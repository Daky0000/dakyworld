import { chromium } from 'file:///C:/Users/ASUS/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
import {mkdirSync,writeFileSync} from 'node:fs';
const routes=['/','/leads','/hunts','/lead-sources','/leads/import','/clients','/proposals','/demos','/concepts','/projects','/invoices','/care-plans','/emails','/inbox','/messages','/agents','/approvals','/rehearsals','/agents/tools','/costs','/website','/products/pricing','/team','/settings','/website/sites','/website/assets','/website/compatibility','/website/survey','/website/onboarding','/website/ai','/website/updates','/website/team','/website/audit','/website/settings','/website/source','/website/billing'];
mkdirSync('.design-review/live',{recursive:true});
const browser=await chromium.launch();const page=await browser.newPage({viewport:{width:1440,height:1000}});
const results=[];let errors=[];page.on('pageerror',e=>errors.push(e.message));
for(const route of routes){errors=[];await page.goto('http://localhost:5173'+route,{waitUntil:'domcontentloaded'});await page.waitForTimeout(1100);const state=await page.evaluate(()=>({headings:[...document.querySelectorAll('main h1,main h2')].map(x=>x.textContent).slice(0,9),overflow:document.querySelector('main')?.scrollWidth>document.querySelector('main')?.clientWidth,empty:!document.querySelector('main')?.textContent?.trim(),controls:document.querySelectorAll('main button,main input,main select').length}));await page.screenshot({path:'.design-review/live/'+(route.slice(1).replaceAll('/','-')||'dashboard')+'.png'});results.push({route,...state,errors:[...errors]});console.log(JSON.stringify(results.at(-1)));}
writeFileSync('.design-review/live/audit.json',JSON.stringify(results,null,2));await browser.close();
