{
  "$schema": "https://railway.app/railway.schema.json",
  "_comment": "Only read when Railway's Root Directory is the repo root. It is currently set to `server`, so server/railway.json is what actually applies — keep the two in step.",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/api/ready",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE"
  }
}
