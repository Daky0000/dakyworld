node_modules/
dist/
build/
# Temp copies of a page, made to screenshot it with the reveal CSS stripped.
__tmp_*.html
.env
.env.local
*.log
.DS_Store
server/prisma/dev.db
*.tsbuildinfo
# The generated intermediate for the workflow PDF. The sources in
# docs/workflow/ and the PDF itself are tracked; this is neither, and it
# carries 136 KB of base64 fonts that churn the diff on every rebuild.
server/docs/agent-master-workflow.html
# Same for the complete reference — the PDF is tracked, the intermediate is not.
server/docs/dakyworld-os-reference.html
# The same for the operations picture and the two fragments of the bound
# volume: sources in docs/workflow/ and the PDFs are tracked, the intermediates
# carry the base64 fonts and are not.
server/docs/agent-system-front.html
server/docs/agent-system-current.html
# Python bytecode from the doc-binding script.
server/__pycache__/
# Rendered demo documents — regenerate with `npm run demo:docs`.
server/tmp/
# The volume's own cover and index — an intermediate the binder reprints.
server/docs/agent-system-front.pdf
